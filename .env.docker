APP_ENV=docker

# PostgreSQL Database Credentials
POSTGRES_USER=adminuser
POSTGRES_PASSWORD=adminpass123
POSTGRES_DB=admindb

# SQLAlchemy URL for Postgres (service name `db` from docker-compose)
DATABASE_URL=postgresql+psycopg2://adminuser:adminpass123@admin-db:5432/admindb

# RabbitMQ
RABBIT_URL=amqp://guest:guest@rabbitmq:5672/

# JWT Secret Key
SECRET_KEY=JAMIESKEY

# Auth Service URL (for circuit breaker/login proxy)
AUTH_SERVICE_URL=http://auth-api:8000

